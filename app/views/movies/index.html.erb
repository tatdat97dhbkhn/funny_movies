<div class="grid grid-cols-1 lg:grid-cols-2 gap-4"
     data-controller="<%= current_user ? 'movies--index' : '' %>"
     data-movies--index-sender-id-value="<%= current_user&.id %>"
>
  <%= render partial: 'movies/movie', collection: @movies %>
</div>
